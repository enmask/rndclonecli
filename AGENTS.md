## Long-term direction
Long-term, this project may support multiple engine timing models (for example synchronous and asynchronous).

## Current instruction
Do not introduce engine abstractions, new classes, or multi-module refactors for this goal unless explicitly asked.
For now, prefer small changes that improve testability and centralize update logic.
